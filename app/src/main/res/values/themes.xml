<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Nicestart" parent="Theme.Material3.DayNight">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.Nicestart.ActionBar" parent="Theme.Material3.DayNight">
        <item name="colorPrimary">@color/g1</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="background">@android:color/transparent</item>
        <item name="android:elevation">0dp</item>
    </style>

    <style name="TransparentActionBar" parent="Widget.AppCompat.ActionBar">
        <item name="background">@android:color/transparent</item>
        <item name="android:elevation">0dp</item>
    </style>

    <style name="Theme.Nicestart.NoActionBar" parent="Theme.Material3.DayNight.NoActionBar">
<!--        No iniciar el lanzador por defecto-->
        <item name="android:windowIsTranslucent">true</item>

    </style>
  <!-- Base application theme. -->
    <style name="Base.Theme.Nicestart.NoActionBar" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

</resources>