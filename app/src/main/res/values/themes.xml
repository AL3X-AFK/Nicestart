<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Nicestart" parent="Theme.Material3.DayNight">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.Nicestart.ActionBar" parent="Theme.MaterialComponents.DayNight">
        <item name="colorPrimary">@color/g2</item>
        <item name="colorPrimaryVariant">@color/g4</item>
        <item name="colorSecondary">@color/g3</item>
        <item name="android:textColor">@color/white</item>
    </style>

    <style name="Theme.Nicestart.NoActionBar" parent="Theme.Material3.DayNight.NoActionBar" />

</resources>