<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher_shark"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_shark_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Nicestart.NoActionBar">
        <activity
            android:name=".MainBab"
            android:exported="true"
            android:theme="@style/Theme.Nicestart.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
        <activity
            android:name=".githubprofile"
            android:exported="false"
            android:parentActivityName=".MainActivity"
            android:theme="@style/Theme.Nicestart.ActionBar" />
        <activity
            android:name=".ProfileActivity"
            android:exported="true" />
        <activity
            android:name=".Splash"
            android:exported="false">

        </activity>
        <activity
            android:name=".Signup"
            android:exported="false"
            android:parentActivityName=".Login"
            android:theme="@style/Theme.Nicestart.ActionBar" />
        <activity
            android:name=".Login"
            android:exported="true" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.Nicestart.ActionBar" />
    </application>

</manifest>